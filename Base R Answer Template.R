
# STUDENT ID:
student_id<- # write your student id
# GROUP NAME:
group<-  # write your group name

# Provide the correct answers by filling in the gaps.

#i.
my.num = _(_,_,_,_,_,) # We expect to use the appropriate command and 6 numbers in the brackects
#ii.
_ _ 4 # We expect you to write the appropriate code to multipply my.num by 4
#iii.
my.char = c(_, _, _, _, _) # we expect 5 character strings in the order Robert, Parker, Robert, Robert, Parker
#iv.
both = _(my.num, my.char) # fill in the  gap
#v
_(both) # write the appropriate code to find the length of 'both'
#vi.
_(both) # Determine the class of 'both'
#vii.
_ _ 3 # We expect you to write the appropriate code to divide both by 3
#viii
x = c(1,_,3,_,5,6) # fill in the two gaps
#ix
y =  c(10,_,_,40,_) # We expect three numbers
#x
x + y # Does this code give an answer? We expect either "Yes" or "No" 

#xi.
y = c(_, 60) # fill in the gap.
#xii
x + y # Does this code give an answer? We expect either "Yes" or "No" 
#xiii
_ - _  # we expect you to multiply x by y







